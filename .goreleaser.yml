project_name: kafka-cronsumer
release:
  github:
    name: kafka-cronsumer
    owner: Trendyol
before:
  hooks:
    - go mod tidy
builds:
  - skip: true
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
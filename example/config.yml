dev:
  kafka:
    servers: "localhost:9092"
    consumer:
      group: "sample-consumer"
      exceptionTopic: "exception"
      maxRetry: 3
      concurrency: 1
      cron: "*/2 * * * *"
      durationMinute: 1m